interface:
  display_name: "Fased Release Manager"
  short_description: "Keep Fased fixes focused and releases explicit"
  default_prompt: "Use $fased-release-manager for the newest request only; default to the smallest focused fix and enter lifecycle or release work only when explicitly required."
