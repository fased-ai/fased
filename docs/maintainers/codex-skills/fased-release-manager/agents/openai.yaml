interface:
  display_name: "Fased Release Manager"
  short_description: "Close Fased fixes locally before release"
  default_prompt: "Use $fased-release-manager to close affected Fased predicates through one tag-free LOCAL0 proof before any RC allocation and preserve explicit irreversible boundaries."
