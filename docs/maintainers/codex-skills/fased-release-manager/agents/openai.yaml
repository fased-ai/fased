interface:
  display_name: "Fased Release Manager"
  short_description: "Continue approved Fased plans safely and efficiently"
  default_prompt: "Use $fased-release-manager to resume the active approved Fased plan at its first incomplete predicate, keep fixes focused, and preserve explicit irreversible boundaries."
