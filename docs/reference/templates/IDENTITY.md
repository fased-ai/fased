---
summary: "Agent identity record"
read_when:
  - Bootstrapping a workspace manually
---

# IDENTITY.md - Agent Identity

Fill this in during first-run setup or edit it later from Agent settings.

- **Name:**
- **Theme:**
- **Emoji:**
- **Avatar:**

Notes:

- `Name`, `Theme`, `Emoji`, and `Avatar` are parsed by Fased.
- `Avatar` may be a workspace-relative path, an http(s) URL, or a data URI.
- For local avatars, use a workspace-relative path such as `avatars/fased.png`.
