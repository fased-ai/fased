# Rootless CI cannot execute Rocky's setuid unix_chkpwd helper. Authorization
# still comes from the fixture-only NOPASSWD sudoers policy created by run.sh.
auth       sufficient   pam_permit.so
account    sufficient   pam_permit.so
password   sufficient   pam_permit.so
session    optional     pam_permit.so
