# Rootless CI containers cannot execute Rocky's setuid unix_chkpwd helper.
# Preserve the real systemd user manager while leaving host PAM outside H0.
[Service]
PAMName=
Environment=XDG_RUNTIME_DIR=/run/user/%i
Environment=DBUS_SESSION_BUS_ADDRESS=unix:path=/run/user/%i/bus
